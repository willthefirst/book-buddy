import React, { Component } from 'react'

class Daily extends Component {

  // Request current books for that daily
    // If entry has books in it, render those books (for past entries that have already been made)
    // else just render user's 'current' books

  render () {


    return (
      <div>Daily</div>
    )
  }
}

Daily.propTypes = {

}

export default Daily
